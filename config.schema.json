{
  "pluginAlias": "LifxCeiling",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Split LIFX Ceiling uplight and downlight into two full-colour HomeKit lights. Local LAN control only \u2014 no LIFX cloud, no LIFX bridge.\n\n**Works out of the box:** install it, and every LIFX Ceiling on the LAN (vendor 1, pid 176 US / pid 177 Intl) appears as two lights \u2014 Uplight and Downlight \u2014 each with full colour, brightness and colour-temperature control.\n\n**The geometry is fixed, on purpose.** On this product the uplight is driven by the **four corner cells** of the 8\u00d78 matrix \u2014 `0, 7, 56, 63` \u2014 and the downlight by the other 60. Confirmed on four separate fixtures by painting cells and reading the panel, so it is a constant in `lib/matrix.js` (`CEILING_UPLIGHT_CELLS`) with nothing to configure: no file, no path, no per-fixture override. Other matrix products are unsupported, and that is a deliberate product decision, not a gap to fill. If a colour you sent to the uplight shows on the panel face, run `npm run doctor` and open a hardware report.\n\n**Tile writes go out as service 715 with `ack_required: true`.** Un-acked frames vanish on this hardware with no error, so every write waits for its acknowledgement and treats service 223 as a failure.\n\n**DHCP reservations are no longer required.** Accessory identity is the fixture's hardware MAC, persisted in the accessory cache, so a fixture that renews onto a new address keeps the same UUID, name and room \u2014 only its transport moves. A reservation is still *nice*: the logs stay tidy and reconnects are marginally faster.",
  "footerDisplay": "Both halves address the same physical fixture, so a fixture's power is only turned off when both halves are off.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "LIFX Ceilings"
      },
      "aliases": {
        "title": "Name aliases (MAC -> short name)",
        "type": "object",
        "required": false,
        "description": "**Optional, site-local.** Short names for your fixtures: { \"aa:bb:cc:dd:ee:ff\": \"SW\" } publishes \"SW Uplight\" and \"SW Downlight\". Keys are MACs \u2014 case and : separators are free \u2014 because a MAC survives an IP change. Unset, a tile is named from the label the fixture carries in the LIFX app, and a name you renamed in Home.app is left alone across restarts. Set an alias and it is republished every boot, because you asked for it. A key that is not a MAC is refused with a named warning.",
        "additionalProperties": {
          "type": "string",
          "minLength": 1,
          "maxLength": 32
        }
      },
      "kelvinMin": {
        "title": "Minimum colour temperature (K)",
        "type": "integer",
        "minimum": 1000,
        "maximum": 9000,
        "default": 1500,
        "required": false,
        "description": "Warmest white the fixture will go. Clamped in software and published to HomeKit as the colour-temperature limit."
      },
      "kelvinMax": {
        "title": "Maximum colour temperature (K)",
        "type": "integer",
        "minimum": 1000,
        "maximum": 9000,
        "default": 9000,
        "required": false,
        "description": "Coolest white the fixture will go. Clamped in software and published to HomeKit as the colour-temperature limit."
      },
      "discoveryInterval": {
        "title": "Discovery interval (seconds)",
        "type": "integer",
        "minimum": 60,
        "default": 300,
        "required": false
      }
    }
  }
}
